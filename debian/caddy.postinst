#!/bin/sh

set -e

case "$1" in
    configure)
        adduser --system --disabled-password --disabled-login --home /var/www-data \
                --quiet --force-badname --group www-data
        ;;
esac

#DEBHELPER#

exit 0
